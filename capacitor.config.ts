import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic_week09kpe',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
